# 2026-08-10

## Memos 页面三项改动

修改文件：
- `src/pages/memos.astro` — 移除外层 card 包裹和静态标题，仅保留 MemosList 组件
- `src/components/memos/MemosList.svelte` — 三项改动：

1. **移除 memos.flygeon.top 链接**：工具栏中不再显示指向 memos 站点的外链，仅保留刷新按钮和更新时间。
2. **图片骨架占位图**：memo 附件图片加载时，父容器显示 `animate-pulse` 灰色骨架背景，图片 `opacity:0`；`on:load` 后切换为 `opacity:1` 并移除骨架类；`on:error` 显示"图片加载失败"。
3. **复刻 ankyu.top header 卡片**：在 MemosList 顶部新增 `<header>` 卡片，grid 两列布局（`1fr_auto`），左侧标题"动态"+副标题+动态计数，右侧年份筛选下拉框，圆角使用 `--radius-large`。

构建验证通过（`astro build` 零错误）。
