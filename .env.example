# ===== YuuComments 前端配置（公开变量，会注入到客户端 JS）=====
# 复制为 .env 后填写；Cloudflare Pages 请在项目设置 -> Environment variables 中
# 以相同变量名配置（PUBLIC_ 前缀的变量会在构建时暴露给浏览器端 JS）。

# 评论后端 Worker 的地址（自定义域名）。注意：不要带结尾斜杠。
PUBLIC_COMMENTS_API_BASE_URL=https://comments.flygeon.top

# Cloudflare Turnstile 站点密钥（前端可见）。
# 在 Cloudflare 控制台 -> Turnstile 创建 widget，Allowed hostnames 填 flygeon.top 与 www.flygeon.top。
PUBLIC_TURNSTILE_SITE_KEY=你的_turnstile_site_key

# ===== 后端 Worker 需要的密钥（仅服务端，不要放进前端 .env）=====
# 以下两项在 YuuComments 仓库用 `wrangler secret put` 设置，不要写在这里：
#   ADMIN_TOKEN          后台管理令牌（用于删除/封禁评论）
#   TURNSTILE_SECRET_KEY Turnstile 服务端密钥（与上面的站点密钥配对）

# ===== Memos 动态页（Pages Function 运行时使用，不注入客户端 JS）=====
# /api/memos 由 functions/api/memos.ts 代理拉取，token 只存放在服务端，不会暴露到浏览器。
# 在 Cloudflare Pages 项目设置 -> Environment variables 配置（Production 与 Preview 环境都要），
# 注意勿加 PUBLIC_ 前缀。构建本身不再需要此变量。
# 本地测试函数：`npx wrangler pages dev dist --binding MEMOS_TOKEN=xxx`
MEMOS_TOKEN=memos_pat_你的_token
