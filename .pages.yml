media:
  input: /image
  output: https://raw.flygeon.eu.org/Flygeon/Astro/refs/heads/main/image/
  rename: safe
  extensions: [png, jpg, jpeg, webp, gif, svg]

content:
  - name: posts
    label: 文章
    type: collection
    path: src/content/posts
    format: yaml-frontmatter
    subfolders: true

    fields:
      - name: title
        label: 标题
        type: string
        required: true

      - name: published
        label: 发布日期
        type: date
        required: true

      - name: body
        label: 正文
        type: rich-text
        required: true

      - name: description
        label: 摘要
        type: text
        required: false

      - name: image
        label: 封面图片 URL
        type: string
        required: false
        hint: 输入完整图片链接

      - name: tags
        label: 标签
        type: string
        list: true
        required: false

      - name: category
        label: 分类
        type: string
        required: false

      - name: draft
        label: 草稿
        type: boolean
        default: false

      - name: updated
        label: 更新日期
        type: date
        required: false

      - name: pinned
        label: 置顶
        type: boolean
        default: false

    view:
      primary: title
      sort: published
      order: desc
      fields: [create, title, published, draft, tags]

    filename:
      template: "{primary}.md"
      field: title